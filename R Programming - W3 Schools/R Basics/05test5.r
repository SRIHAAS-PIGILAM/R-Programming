name <- "Jhon"
print(name)

# Create two strings
string1 <- "Hello"
string2 <- "World"

# Use the paste function to concatenate the strings
result <- paste(string1, string2)

# Print the result
cat("Concatenated String:", result)

# We define two strings, string1 and string2.

# We use the paste function to concatenate the two strings, separated by a space. The result is stored in the result variable.

# We use cat to print the concatenated string to the console.
